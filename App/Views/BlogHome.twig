
{% extends "Layout.twig" %}


{% block title %}
    <title>Blog - Accueil</title>
{% endblock %}

{% block description %}
    <meta name="description" content=""/>
{% endblock %}

{% block content %}

    <header>
        
        <div id="header-wrapper">
        
            {% include ("Nav_bar.twig") %}

            <div class="intro_blog">
                <p>BLOG</p>
                {# <a href="#" id="chevron"> #}
                <i class="fa fa-chevron-down" id="chevron"></i>
                {# </a> #}
            </div>
            
        
        </div>
    </header>

    <section id="blog">

        <div id="posts">
            {% for article in articles %}
            <article>
                <div class="post">
                    <div class="img_bg" style="background-image:url({{article.url_image}})"></div>
                </div>
                <div class="post_infos">
                    <span>🤵🏻 {{article.author}}</span>
                    <span>🗓️ {{article.date_create|date("d/m/Y")}}</span>
                </div>
                <h1>{{article.title}}</h1>
                <p>Lorem Ipsum is simply dummy text of the printing and typesetting industry. Lorem Ipsum has been the industry's standard dummy text ever since the 1500s, when an unknown printer took a galley of type and scrambled it to make a type specimen book.</p>
                <a href="{{URL}}blog/{{article.slug}}-{{article.id}}"><button class="button-blue">Lire l'article</button></a>
            </article>
            {% endfor %}
        </div>



        <div id="pagination">

            {% if current_page >= 2 %}

                <a href="{{URL}}?page={{current_page - 1}}#blog"><button class="button-blue">Page précédente</button></a>

            {% endif %}

            {% if current_page < pages_total %}

                <a href="{{URL}}?page={{current_page + 1}}#blog"><button class="button-blue">Page suivante</button></a>

            {% endif %}

        </div>

    </section>

    

    
{% endblock %}

{% block script %}
    <script src="{{URL}}Public/js/scroll_to_blog.js"></script>
{% endblock %}